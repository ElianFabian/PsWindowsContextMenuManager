function Split-WindowsContextMenuKeyPath {
    
    [OutputType([string])]
    [CmdletBinding()]
    param (
         [string[]] $LiteralKeyPath,

         [switch] $Parent,

         [switch] $Leaf
    )

    # TODO
}